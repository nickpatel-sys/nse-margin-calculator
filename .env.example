# Copy this to .env and fill in real values before deploying.
# docker-compose.yml reads these automatically.

# Required: change to a long random string in production.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-before-going-public
